<x-app-layout :hide-navigation="true">
    <x-slot name="header"><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-[10px] font-black uppercase tracking-[.25em] text-indigo-500">Detalle paginado</p><h2 class="text-2xl font-black text-gray-900 dark:text-white">{{ $title }}</h2></div><a href="{{ route('admin.device-audit.reimbursements-dashboard', ['section' => $report === 'approvers' || $report === 'centers' ? 'centers' : 'general', 'range' => $range, 'company' => $companyId, 'cost_center' => $costCenterId]) }}" class="rounded-xl bg-gray-900 px-4 py-2 text-sm font-bold text-white dark:bg-white dark:text-gray-900">Volver al dashboard</a></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-5 px-4 sm:px-6 lg:px-8">
        <form method="GET" class="grid gap-3 rounded-2xl border border-gray-200 bg-white p-4 shadow-sm md:grid-cols-4 dark:border-gray-700 dark:bg-gray-800"><input name="search" value="{{ $search }}" placeholder="Buscar" class="rounded-xl border-gray-300 text-sm dark:border-gray-600 dark:bg-gray-900 dark:text-white"><select name="company" class="rounded-xl border-gray-300 text-sm dark:border-gray-600 dark:bg-gray-900 dark:text-white"><option value="">Todas las empresas</option>@foreach($companies as $company)<option value="{{ $company->id }}" @selected($companyId === $company->id)>{{ $company->name }}</option>@endforeach</select><select name="cost_center" class="rounded-xl border-gray-300 text-sm dark:border-gray-600 dark:bg-gray-900 dark:text-white"><option value="">Todos los centros</option>@foreach($costCenters as $center)<option value="{{ $center->id }}" @selected($costCenterId === $center->id)>{{ $center->code }} · {{ $center->name }}</option>@endforeach</select><button class="rounded-xl bg-indigo-600 px-4 py-2 text-sm font-black text-white">Buscar y filtrar</button></form>
        <div class="overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-sm dark:border-gray-700 dark:bg-gray-800"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-left text-xs uppercase tracking-wide text-gray-500 dark:bg-gray-900">@if(in_array($report, ['amounts','categories']))<tr><th class="px-5 py-3">Folio</th><th class="px-4 py-3">Solicitante</th><th class="px-4 py-3">Centro / categoría</th><th class="px-4 py-3">Estatus</th><th class="px-4 py-3 text-right">Monto</th></tr>@elseif($report === 'approvers')<tr><th class="px-5 py-3">Centro de costos</th><th class="px-4 py-3">Etapa</th><th class="px-4 py-3">Aprobador</th><th class="px-4 py-3">Correo</th></tr>@else<tr><th class="px-5 py-3">Centro</th><th class="px-4 py-3">Empresa</th><th class="px-4 py-3 text-right">Reembolsos</th><th class="px-4 py-3 text-right">Monto</th></tr>@endif</thead><tbody class="divide-y divide-gray-100 dark:divide-gray-700">@forelse($items as $item)@if(in_array($report, ['amounts','categories']))<tr><td class="px-5 py-3 font-mono text-xs">{{ $item->folio ?: '#' . $item->id }}</td><td class="px-4 py-3">{{ $item->user?->name ?: '—' }}</td><td class="px-4 py-3">{{ $item->costCenter?->name ?: '—' }}<br><small class="text-gray-500">{{ $item->category ?: 'Sin categoría' }}</small></td><td class="px-4 py-3">{{ str($item->status)->replace('_', ' ')->title() }}</td><td class="px-4 py-3 text-right font-bold">${{ number_format($item->total + $item->propina, 2) }}</td></tr>@elseif($report === 'approvers')<tr><td class="px-5 py-3">{{ $item->costCenter?->code }} · {{ $item->costCenter?->name }}</td><td class="px-4 py-3">{{ $item->order }}. {{ $item->name }}</td><td class="px-4 py-3 font-bold">{{ $item->user?->name ?: 'Sin asignar' }}</td><td class="px-4 py-3">{{ $item->user?->email ?: '—' }}</td></tr>@else<tr><td class="px-5 py-3 font-bold">{{ $item->code }} · {{ $item->name }}</td><td class="px-4 py-3">{{ $item->company?->name ?: '—' }}</td><td class="px-4 py-3 text-right">{{ $item->reimbursements_count }}</td><td class="px-4 py-3 text-right">${{ number_format($item->reimbursements_sum_total ?: 0, 2) }}</td></tr>@endif @empty <tr><td colspan="5" class="px-5 py-10 text-center text-gray-500">Sin resultados.</td></tr>@endforelse</tbody></table></div><div class="border-t border-gray-200 px-5 py-4 dark:border-gray-700">{{ $items->appends(request()->query())->links() }}</div></div>
    </div></div>
</x-app-layout>
